# 📘 Math World

Автоматизований pipeline для генерації теорії з PDF-підручників математики для учнів 5 класу.

## Локальна модель Ollama (bakllava) для геометрії

- Запустіть модель: `ollama run bakllava` (порт 11434 за замовчуванням).
- Стартуйте воркер: `celery -A llm_worker worker --loglevel=info`.
- Якщо пункт містить геометричні ключові слова, воркер попросить модель побудувати SVG-рисунок і збереже його в `media/generated/`.
- Шаблон перегляду пункту показує спочатку теорію, потім практику, і додає рисунок для геометричних задач.

### Як це працює і чи безкоштовно

- Використовується **локальна** модель `bakllava` через Ollama, тож жодні зовнішні API не викликаються й плата за запити відсутня.
- Потрібні лише встановлений Ollama і ресурс вашої машини (CPU/GPU + дисковий простір для моделі); витрати — це час обчислень і електроенергія.
- Воркери Celery слухають базу задач: коли з’являється геометричний пункт, формується prompt, модель генерує SVG, а шлях до файлу зберігається в БД для відображення в UI.

### Імпорт змісту підручників до бази

Текстові файли у форматі `data/<номер>_class_*.txt` можна завантажити в моделі
`Grade/Section/Paragraph/Item` командою:

```bash
python manage.py import_curriculum --directory data
```

Для окремого файлу, де назва не містить номера класу, вкажіть `--file` та
`--grade`:

```bash
python manage.py import_curriculum --file data/5_class_ukr.txt --grade 5
```

Якщо потрібно завантажити файл наново і прибрати попередні дані цього класу,
додайте `--reset` — команда видалить існуючий запис класу разом із усіма
розділами/параграфами/пунктами й імпортує файл «з нуля»:

```bash
python manage.py import_curriculum --file data/5_class_ukr.txt --grade 5 --reset
```

## 🔧 Скрипти

- `run_pipeline.py`: головний скрипт (OCR + LLM + збереження в базу)
- `archive/`: старі скрипти, переміщені з проєкту

## 📦 Використання
```bash
python run_pipeline.py


---

## ✅ Кроки для тебе:

1. Створи **нову гілку** в своєму репозиторії:
   ```bash
   git checkout -b feature/run-pipeline
